# 术语清单与技术解释

本文为本仓库两份研究方案的辅助读物，面向具备数据结构、线性代数、概率论和基础深度学习知识的计算机科学本科生。它解释文档中的术语，而不替代实验结果。

> 重要边界：本项目的研究计划尚未通过实验验证。尤其是“DRPI 能否改善路径保持或减少副作用”是待检验假设，不是已知结论。

## 先用一句话建立全局图景

普通神经网络通常让每层都用同一套参数计算。MoE 模型把一层拆成许多“专家”小网络，并让一个 router 为每个 token 挑少数专家。项目关注的问题是：若我们只改变模型内部状态中“当前 router 看不见”的部分，后面的 router 会不会仍然看见这个变化？DRPI 尝试寻找既不改变当前选择、又尽量不改变后续选择的干预方向。

## 一、模型与路由

| 术语 | 英文/符号 | 本科生级解释 | 在本项目中的作用 |
|---|---|---|---|
| 大语言模型 | Large Language Model, LLM | 根据前文预测下一个 token 的神经网络。它先把文字变成向量，再经多层计算输出概率分布。 | DRPI 的实验载体。 |
| Token | token | 模型处理文本的最小片段，可能是一个字、词的一部分或标点，不等于自然语言中的“一个词”。 | 路由、干预和路径比较都要说明具体 token 位置。 |
| 隐藏状态 | hidden state, \(h\) | 模型在某层为一个 token 保存的高维数值向量，可粗略理解为该 token 此时的内部表示。 | 干预 \(\delta\) 加在这里或其实际派生张量上。 |
| Transformer | Transformer | 现代 LLM 的常见网络结构。其核心部件是 attention、前馈网络、残差连接和归一化。 | MoE 通常把其中的前馈网络替换或扩展为专家层。 |
| 注意力 | attention | 一个 token 根据相关程度读取其他 token 信息的机制。它会使早期某个位置的变化间接影响其他位置。 | 是局部干预可能在后续层扩散的原因之一。 |
| MoE | Mixture of Experts | “专家混合”架构：一层中有多个小网络，但每个 token 只激活其中少数几个，兼顾总参数量和计算量。 | 项目的研究对象。 |
| 专家 | expert | MoE 层里的一个子网络，通常类似前馈网络。不同 token 可以由不同专家处理。 | 被选择的专家序列构成计算路径的一部分。 |
| Router / Gate | 路由器 / 门控网络 | 一个较小的网络，根据 hidden state 给每位专家打分并选择少数专家。 | 决定当前 token 在 MoE 层调用哪些专家。 |
| Router 输入 | router input | 实际送入 router 的张量。它可能是 hidden state，也可能已经过 RMSNorm 等变换。 | 干预必须精确挂在这里，不能只凭模块名称猜测。 |
| Expert 输入 | expert input | 实际送入被选专家的张量。 | 只有 router 与 expert 共用输入时，才适合讨论 shared-state intervention。 |
| Logit | logit, \(z\) | softmax 之前的原始分数。分数越大，通常越容易被选中。 | Router 为每个专家输出一个 logit。 |
| Top-k 路由 | top-k routing | 对每个 token，只选 router 分数最高的 \(k\) 个专家。 | “路径是否保持”的基本比较对象。 |
| Gate weight | 门控权重 | 在选出的专家之间分配输出比例的权重，通常由选中专家的 logits 归一化得到。 | top-k 不变不一定意味着权重不变，所以应单独检查。 |
| 专家路径 | expert route/path | 一个 token 逐层被哪些专家选中的离散记录。 | 项目试图测量和控制的对象。 |
| 稀疏计算 | sparse computation | 总共有很多专家，但每次只计算少数几个，因此实际计算量较小。 | 解释了为什么路由选择既影响效率，也可能影响模型行为。 |
| 残差连接 | residual connection | 把某层输入直接加到输出的通路，让信息和梯度更容易穿过深层网络。 | 会把干预带到后续层，即使当前 router 没有感知它。 |
| RMSNorm / LayerNorm | 均方根归一化 / 层归一化 | 调整向量数值尺度的运算，使训练更稳定。它不是普通线性变换。 | 若在 norm 前注入，原本的零空间关系可能被改变。 |

## 二、线性代数与干预

| 术语 | 英文/符号 | 本科生级解释 | 在本项目中的作用 |
|---|---|---|---|
| Router 权重矩阵 | router weight, \(R_l\) 或 \(W\) | 线性 router 的参数矩阵。若 hidden state 是 \(h\)，最简单情形下 logits 为 \(Rh+b\)。 | 用它构造当前层的静态盲空间。 |
| 第 \(l\) 层 | layer \(l\) | 从输入到输出依次编号的网络层。 | 注入层记为 \(l\)，后续层常记为 \(k>l\)。 |
| 扰动 / 干预 | perturbation / intervention, \(\delta\) | 人为加到内部状态的小向量，用来观察或改变模型行为。 | 研究对象不是自然误差，而是可控的状态修改。 |
| 激活干预 | activation steering | 在模型中间向量上加某个方向，以影响输出的技术。 | DRPI 是受路由约束的激活干预；普通 steering 是强基线。 |
| 零空间 / 核 | null space / kernel, \(\ker(R)\) | 所有满足 \(R\delta=0\) 的向量集合。沿这些方向变化后，线性映射 \(R\) 的输出不变。 | 保守定义下，\(\delta\in\ker(R_l)\) 不改变当前 router logits。 |
| 静态盲空间 | static blind space | 当前层 router 看不见的方向集合。“静态”表示只针对一个固定层、一个固定时刻。 | 是 DRPI 的可行动作空间，不是整个网络的永久不变空间。 |
| 可见方向 | route-visible direction | 会改变当前 router logits 的方向。 | 与 blind 投影对照，用于检查路由敏感性。 |
| 投影 | projection | 把一个向量分解后，只保留指定子空间内的部分。例如把目标方向投到零空间。 | 将原始方向 \(d\) 变成满足当前层约束的 \(\delta\)。 |
| 正交 | orthogonal | 两个向量内积为零，可直观理解为互相垂直。 | 用正交基和投影把“危险方向”从目标方向中移除。 |
| 正交基 | orthonormal basis, \(B_l\) | 一组长度为 1、彼此垂直的向量，能表示一个子空间中的任意向量。 | \(\delta_l=B_lu\) 使任意坐标 \(u\) 都留在静态盲空间。 |
| 中心化 | centering, \(C\) | 从所有专家 logits 中减去共同平均值。给每个专家同时加同一个常数不会改变相对大小。 | \(\ker(CR_l)\) 比 \(\ker(R_l)\) 更宽松，但需额外验证。 |
| 公共平移 | common logit shift | 全部 logits 同时加同一常数。softmax 概率和排序都不变。 | 解释为何中心化 null space 可能保留更多方向。 |
| SVD | singular value decomposition | 把矩阵拆成几个互相垂直的重要方向及其强度的线性代数工具。 | 用于求零空间基，也用于寻找下游路由最敏感的低秩方向。 |
| 秩 | rank | 一个矩阵中真正独立方向的数量。 | 决定 router 约束多少维度，以及剩余盲空间有多大。 |
| 低秩近似 | low-rank approximation | 只保留最重要的少数方向，忽略影响较小的部分。 | 让 DRPI 不必储存或处理全部下游梯度。 |
| 危险子空间 | dangerous subspace | 在当前层盲空间内部，那些一旦改动就容易让后续路由边界翻转的方向。 | DRPI 试图避开它；它是数据条件下的近似对象。 |
| 动态盲空间 | dynamic blind space | 在给定输入分布和有限下游范围内，对未来路由也尽量不敏感的方向。 | 是 DRPI 试图近似的对象，不存在默认的全局严格保证。 |
| 线性化 | linearization | 在一个很小的邻域内，用直线或矩阵近似复杂非线性函数。 | 用梯度预测小干预对后续 margin 的影响。 |
| 局部不变性 | local invariance | 某个性质只在一个位置、一个时刻或很小扰动范围内保持。 | router-null 的严格结论属于这一类。 |
| 动态不变量 | dynamic invariant | 一个性质经系统多步演化后仍始终保持。 | 本项目要检验静态盲空间通常是否不是这种不变量。 |

## 三、梯度、边界与优化

| 术语 | 英文/符号 | 本科生级解释 | 在本项目中的作用 |
|---|---|---|---|
| 路由 margin | routing margin, \(m\) | 最弱的已选专家分数减去最强的未选专家分数。它越小，两个专家越容易交换位置。 | 是预测和约束未来专家切换的关键量。 |
| 决策边界 | decision boundary | 改变输入后，模型从一种离散选择切换到另一种选择的临界位置。 | 当某个路由 margin 从正数跨过 0，top-k 集合会改变。 |
| 梯度 | gradient, \(g\) | 函数对每个输入维度的局部变化率；指出让函数增长最快的方向。 | \(g_a=\nabla_{h_l}m_a\) 表示下游某条 margin 对注入状态的敏感性。 |
| Jacobian | Jacobian, \(J\) | 向量函数的一阶导数矩阵，可看作“许多梯度按行堆叠”。 | 近似描述注入层状态如何影响后续 router logits。 |
| 一阶近似 | first-order approximation | 用 \(f(x+\delta)\approx f(x)+\nabla f(x)^T\delta\) 估计小变化。 | 用 \(m+g^T\delta\) 预测是否会发生路由切换。 |
| 有限差分 | finite difference | 实际把输入加上很小的 \(\epsilon v\)，用输出变化除以 \(\epsilon\) 近似导数。 | 用来确认计算出的梯度和 hook 点正确。 |
| 约束优化 | constrained optimization | 在必须满足一些条件时，寻找让目标最好的解。 | 一种 DRPI 版本：保持当前盲性并要求未来 margin 不低于阈值。 |
| 软约束 | soft constraint | 不硬性禁止违反条件，而是在目标函数中加入惩罚。 | 可在目标效果与路径稳定之间扫描权衡。 |
| 硬约束 | hard constraint | 解必须满足的条件，例如 \(m+g^T\delta\ge\epsilon\)。 | 更直接地防止线性化预测下的边界翻转。 |
| 正则化 | regularization | 在优化目标中惩罚过大或过复杂的解，防止只适用于训练样本。 | 例如惩罚 \(\lVert\delta\rVert_2^2\) 和下游敏感度。 |
| \(L_2\) 范数 | \(\lVert\delta\rVert_2\) | 向量长度的常见度量，等于各分量平方和开根号。 | 用来限制干预强度，并做公平的范数匹配。 |
| 帕累托前沿 | Pareto frontier | 多目标问题中，无法在不牺牲其他目标的前提下继续改善某一目标的方案集合。 | 比较“目标改变、路径漂移、输出副作用”三者的权衡。 |

## 四、实验、指标与统计

| 术语 | 英文/符号 | 本科生级解释 | 在本项目中的作用 |
|---|---|---|---|
| Baseline | 基线 | 已有或较简单的方法，用来判断新方法是否真的有额外价值。 | DRPI 必须击败静态投影、普通 steering、随机子空间和直接改路由等基线。 |
| Teacher forcing | 教师强制 | 把固定的真实 token 序列逐步喂给模型，而不是让模型自己生成下一个 token。 | 让干预前后 token 位置一一对应，可公平比较纵向路径。 |
| 自由生成 | free generation | 模型根据自己上一步生成的内容继续生成。 | 用于评估任务效果和文本质量，不能直接拿来做未对齐路径比较。 |
| 路径生存率 | route survival | 注入后，某个后续层仍选到与 baseline 相同 top-k 专家的比例。 | 将“局部不变性能维持多深”画成曲线。 |
| Top-k exact match | top-k 完全一致 | 两次路由选到的专家集合和顺序都相同。 | 严格的离散路径保持指标。 |
| Jaccard 相似度 | Jaccard similarity | 两个集合的交集大小除以并集大小，忽略顺序。 | 表示 top-k 专家集合的重叠程度。 |
| Hamming 距离 | Hamming distance | 两个等长序列对应位置不同的个数或比例。 | 用于顺序敏感的专家列表比较；只适用于 token 对齐情形。 |
| JS divergence | Jensen-Shannon divergence | 衡量两个概率分布差异的对称指标，值越小表示越相似。 | 比只看 top-k 更细地观察整个路由分布如何漂移。 |
| Centered-logit 漂移 | centered-logit drift | 去除共同平移后，比较干预前后所有专家相对分数的变化。 | 能在 top-k 尚未翻转时发现路由接近边界的趋势。 |
| AUROC | area under ROC curve | 二分类预测器区分正负样本的能力，0.5 约等于随机，1 为完美区分。 | 评估 margin/Jacobian 预测“会不会切换专家”的排序能力。 |
| Brier score | Brier 分数 | 预测概率与实际 0/1 标签之间平方误差的平均值，越小越好。 | 检查预测概率是否可靠，不只看排序。 |
| 校准 | calibration | 预测“有 70% 概率发生”时，现实中是否真的约 70% 发生。 | 防止高 AUROC 但概率解释失真的结果。 |
| 置信区间 | confidence interval, CI | 用重采样等方法给统计量的不确定性范围。 | 报告 95% bootstrap CI，避免把小样本波动当发现。 |
| Bootstrap | 自助法 | 从现有样本反复有放回抽样，估计统计量的波动。 | 用于效应量和路径差异的置信区间。 |
| 配对比较 | paired comparison | 对同一条 prompt 同时跑两种方法，再比较差异。 | 能降低提示本身难度差异带来的噪声。 |
| 范数匹配 | norm matching | 让不同方法使用相同大小的 \(\delta\)。 | 防止“谁加得更大谁更有效”的不公平比较。 |
| 效果匹配 | effect matching | 调整各方法强度，使它们达到相同目标效果后再比较副作用。 | DRPI 的主要公平比较方式。 |
| 路由效果匹配 | routing-effect matching | 让不同方法达到相同程度的路由变化后，再比较任务效果。 | 分析目标能力与路径变化的关系。 |
| Train / calibration / validation / test | 训练 / 校准 / 验证 / 测试集 | 分别用于构造方向、拟合子空间、选超参数和最后报告结果的数据划分。 | 防止同一批提示同时参与设计和验收，导致过拟合。 |
| 消融实验 | ablation | 有控制地移除或替换一个组件，观察它是否必要。 | 例如去掉危险子空间约束，检查 DRPI 的增益来自哪里。 |
| 非劣界限 | non-inferiority bound | 预先规定质量允许下降的最大幅度。 | 避免只因路径更稳定就接受明显更差的输出。 |

## 五、工程实现与可复现性

| 术语 | 英文/符号 | 本科生级解释 | 在本项目中的作用 |
|---|---|---|---|
| Hook | hook | 在神经网络某个模块运行前或运行后插入自定义代码的机制。 | 用于捕获 router 输入/logits，或在正确位置注入 \(\delta\)。 |
| Forward pre-hook | 前向预钩子 | 模块真正执行前触发的 hook，可以替换输入。 | shared-state 注入的首选实现位置。 |
| Autograd | 自动微分 | PyTorch 自动记录运算图并计算导数的系统。 | 计算下游 margin 对注入状态的梯度。 |
| 计算图 | computation graph | 记录张量如何由前序运算得到的有向图。 | 过早 `detach()` 会断开图，无法求所需梯度。 |
| `detach()` | 脱离计算图 | 让张量不再追踪梯度的操作。 | baseline 落盘可用；构建 DRPI 时不能在求梯度前使用。 |
| Dtype | 数据类型 | 张量数值精度，例如 float32、bfloat16。 | 精度会影响零空间残差、top-k 边界和可复现性。 |
| Full precision / bfloat16 | 全精度 / bfloat16 | 前者通常为 float32；后者范围大、内存较省但精度较低。 | 首轮实验应记录实际精度，避免量化误差混入机制结论。 |
| 模型 revision | model revision | 模型仓库中某个明确的版本或 commit。 | 不同 revision 的结构或权重可变，方向不能随意复用。 |
| 权重哈希 | router weight hash | 对 router 权重计算的固定摘要字符串，用于识别权重是否相同。 | 绑定 DRPI artifact，防止误用到不同模型。 |
| Artifact | 实验产物 | 保存的方向、子空间、报告或图表等可复用输出。 | 每个 artifact 必须带元数据和来源。 |
| Prompt ID | 提示编号 | 每条输入提示的稳定标识。 | 使逐样本结果、划分和失败案例可追溯。 |
| 随机种子 | random seed | 伪随机数生成器的初始值。 | 固定方向采样、token 采样和数据划分，支持复现。 |

## 六、三个最容易混淆的结论

### 1. “当前层没换专家”不等于“整个计算过程没变”

若 \(R_l\delta=0\)，当前 router 的 logits 可以不变；但专家处理后的输出会进入残差流，后面的 router 仍可能看到差异。因此，当前层路由不变只是一个局部代数事实。

### 2. “后续路径相近”不等于“输出一定没有副作用”

即使专家选择相同，专家输入仍可能不同，最终文本或任务行为仍可改变。路径保持可能有用，但它不是输出质量、安全性或隐私的数学证明。

### 3. “梯度预测有效”不等于“DRPI 有全局保证”

梯度只描述当前位置附近的小变化。干预过大、归一化和注意力的非线性、或输入分布改变，都可能使一阶近似失效。必须用独立测试提示和实际路径指标验证。

## 七、推荐阅读顺序

1. 先读 [`极简解释.md`](极简解释.md)，建立问题、方法和意义的直觉。
2. 再回到本文件查询概念。
3. 阅读 [`Idea2_Dynamic_Route_Preserving_Intervention.md`](Idea2_Dynamic_Route_Preserving_Intervention.md) 的第 0–6 节，理解研究定义与方法。
4. 阅读 [`Idea2_Introduction_and_Implementation_Guide.md`](Idea2_Introduction_and_Implementation_Guide.md) 的实现、测试与首轮实验部分。

若需要对公式作更深入的线性代数推导，应以原研究方案中的定义为准；本清单刻意优先解释“它在研究中解决什么问题”。
