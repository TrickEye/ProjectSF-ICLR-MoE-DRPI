> 矩阵乘法有个性质：存在一大类改动方式，乘完打分矩阵结果正好是 0。分数一点不变，那自然还是原来那 8 个专家。
>
> 但这只保证当前这一层。改过的数据继续往下传，下一层的打分器可能就改选别的了。所以核心问题是：这个"看不见"能撑几层？能不能挑一个方向让下面几层也尽量不换？

---

# 我查出来的四条

## 一、"看不见"的范围占 97%，所以没有可测的差别

模型内部数据是 2048 个数，专家 64 个。算下来"打分器看不见"的方向有 1984 个，占 2048 的 96.875%。

意思是：你想改的方向，本来就有 98% 落在"看不见"的范围里。所谓"加了约束的改法"和"不加约束的改法"，几乎是同一个东西。我拿他们自己的代码跑过，两个方向夹角只有 4 度。

论文的主要结论是要比"用新方法"和"不用新方法"哪个好。但这两个几乎是同一件事，比不出差别。而"比不出差别"会被当成科学发现写出去。 这不是 bug，是题目本身没留下可测的空间。

## 二、按他们自己的配置，程序会自己报错停掉

配置里精度设成了 bf16（省内存的低精度）。低精度下"看不见"只能近似成立，误差约 1.5e-2；但代码里写的容许误差是 2e-5，差了近千倍。他们自己的安全检查会抛异常，程序中止。

另一个检查（验证梯度算得对不对）同理：要加的扰动比 bf16 能表示的最小间隔还小，加上去等于没加，检查必然失败退出。

改成 fp32 就能修。但现在的状态是跑不起来。

## 三、衡量"有没有把模型搞坏"的那个指标，恒等于 0

他们只改最后一个词位置的数据，所以只有最后一个位置的输出会变。但算这个指标的代码正好把最后一个位置排除掉了。

所以不管怎么改、改多狠，这个数都是 0.00。报告会显示"质量没下降，置信区间 [0,0]"——看着像好消息，其实是根本没测。另外有一条对照方法也因为这个 bug 退化了，跟另一条变成了同一条。

## 四、这个想法 2026 年已经有四篇论文做过

四篇正文我都拉下来读了：

- RARE：做的就是"往看不见的方向改 + 修正下游漂移"，六个模型都试了，已被 COLM 2026 的 workshop 接收。
- GRIP：用的分数差公式，跟这个仓库 margins.py 里那个一模一样。
- MoEEdit：做的就是"上一层的改动怎么传到下一层打分器，然后把可见部分投影掉"。
- 第四篇最关键：它就在同一个模型（OLMoE-1B-7B）上做的。测"分数差能不能预测专家换掉"——能，0.772。再测"能不能预测这次换掉是好事还是坏事"——不能，等于随机（0.490）。它把 80 个特征一起上，还是随机。

这条最伤。整个项目的价值假设是"少换专家 = 副作用小"。这篇在同一个模型上说：换没换你测得出来，好不好你测不出来。
