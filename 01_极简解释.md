# 极简解释：这个项目在研究什么

## 一句话

MoE 模型会让一个 router 为每个 token 选择少数专家。本项目研究：能不能改变模型内部的一部分状态，同时尽量不改变它之后选择哪些专家；如果不能，原因能否被测量和预测？

## 先想象一个简单场景

把 MoE 想成一个多工位的处理中心。

1. 每个 token 带着一张很长的“内部状态卡”。
2. 每一层的 router 像分流员，根据卡上的内容选出几个专家工位。
3. 这些专家处理后，把结果写回卡片；下一层的分流员再做一次选择。

已有的线性代数告诉我们：在某一层，可以找到某些改卡方式，让**这一层**分流员的打分不变。因此这一层仍会选相同专家。

但这不等于后续分流员也看不出变化。被选专家和后续网络会继续处理这张已经改变的卡，下一层可能改选别的专家。

这正是项目的核心：**“当前层看不见”能维持几层？**

## 主要方法，分三步

### 1. 先做一个局部安全的改动

在注入层，只使用 router 零空间中的方向。直观地说，这些方向经过该 router 的打分规则后变成零，所以当前层的 router logits 不变。

这是严格但很小的保证：只针对正确的注入位置、当前层和当前 token。它不保证后续层、最终输出或任何安全性质。

### 2. 测量变化会不会在后面“回来”

给固定文本做同样的前向计算，在内部加入这类局部改动，然后逐层比较：后续 router 是否还选同一批专家。

这里必须使用固定 token 序列（teacher forcing）。如果让模型自由生成，两个版本很快会写出不同文字，后面的 token 已经不是同一个对象，不能公平比较路由路径。

### 3. DRPI：避开未来最敏感的方向

DRPI 的做法可以理解为两道筛选：

1. 第一道筛选：只保留当前 router 看不见的方向。
2. 第二道筛选：估计哪些剩余方向最容易让后续 router 的专家选择翻转，并尽量避开它们。

估计的工具是梯度。某个后续 router 的两个专家分数非常接近时，这条“边界”很容易翻转；梯度告诉我们，当前状态朝哪个方向轻推最容易让它翻转。DRPI 从目标改动中剔除这些危险成分。

## 它想证明什么，不想证明什么

| 项目希望回答 | 项目不应预先声称 |
|---|---|
| 局部 router-null 改动通常会不会造成下游路由泄漏？ | 当前层路由不变，所以完整计算过程不变。 |
| margin 和梯度能否预测专家何时切换？ | 梯度近似给出全局、永久保证。 |
| DRPI 是否在相同目标效果下更能保持后续路径？ | 路径保持一定降低副作用、提高安全性或保护隐私。 |
| 哪些任务中“改目标但不改路径”可行，哪些不可行？ | router-blind 空间天然就是“内容空间”。 |

目前这些都是研究问题或方法目标；仓库尚无实验结果。

## 为什么有研究意义

MoE 的专家路径是模型内部少见的、可直接观察的离散计算轨迹。若输出改变时路径也改变，很难判断变化来自“内部状态被改了”，还是来自“模型换了一组计算专家”。

因此，能测量并尝试控制路径，有两个价值：

- **机制理解**：弄清局部线性代数结论在深层、非线性网络中究竟能维持多久。
- **干预评测**：更公平地比较不同内部编辑方法的目标效果、路径漂移和非目标副作用。

即使 DRPI 最终不优于简单方法，系统发现“局部不变性在什么条件下失效”本身也可能构成有价值的机制结论。

## 如何判断项目有没有走通

研究不会因为做出一个算法名称就成立。它至少要依次回答：

1. 注入点是否正确，且当前层 router logits 和 top-k 确实不变？
2. 这种局部不变性在后续层是否真的会失效？
3. 小改动下，margin 与梯度是否能预测后续专家切换？
4. 与普通激活干预、静态投影和随机方向相比，DRPI 能否在相同目标效果下更少改变后续路径？
5. 即使路径更稳定，输出质量和非目标行为是否仍然可接受？

任何一步若失败，结论都应相应收缩。例如，如果局部盲性本来就能稳定维持很深，复杂的 DRPI 就没有必要；如果目标改动必须伴随换专家，这也是值得报告的发现。

## 两个阅读入口

- 想查英文词、公式或实验指标：读 [`术语清单与技术解释.md`](术语清单与技术解释.md)。
- 想看完整研究设计与实验要求：读 [`Idea2_Dynamic_Route_Preserving_Intervention.md`](Idea2_Dynamic_Route_Preserving_Intervention.md)。
