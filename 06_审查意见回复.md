# 对《05_审查意见》的回复与项目调整建议

## 0. 总体结论

这份审查意见并非四条都完全准确，但它发现了两个会直接阻断或歪曲真实实验的工程问题，并指出了一项足以改变论文新颖性定位的相关工作。

经核对当前代码、配置和原始论文，结论如下：

1. “blind space 占 97%，所以约束前后不可区分”的维数推理不成立，但 DRPI 相对 static blind 可能只产生很小方向变化的风险是真实的，必须交由效果匹配实验判定。
2. BF16 与当前 router-null 容差、有限差分步长不相容。这是阻断性问题，现有 toy 测试没有覆盖真实 BF16 路径。
3. “所有质量指标都恒为 0”不准确，但 `next_token_nll_delta` 的确对当前最后 token 干预恒不敏感，`retain_loss` 基线因此退化为 `target_only`。`output_kl` 不必为 0，但被全序列平均严重稀释。
4. RARE 与原 DRPI 提案存在实质性重叠，原有“局部 null 不足，需要修正下游路由漂移”不能再单独作为方法新颖性。GRIP 和 MoEEdit 是必须纳入的强相关工作，但与 shared-state inference intervention 并不等价。OLMoE 量化论文否定了“能预测 flip 就能判断 flip 好坏”的推论，但没有证明所有任务中 route mediation 都无效。

因此，当前方案不宜原样继续，也不能在没有外部终点证据时将 DRPI 表述为“减少副作用的新路由保持方法”。

---

## 1. 关于“盲空间占 97%，所以没有可测差别”

### 判断：维数推理不成立，但指出了必须实测的 No-Go 风险

OLMoE 的 hidden size 为 2048，router 有 64 个 expert logits。若 router weight 满行秩，保守 `ker(W)` 的维数是：

$$
2048-64=1984,
$$

占 hidden space 的 96.875%。但是，“96.875% 的维数被保留”不能推出“约束前后功能相同”。被删除的 64 维恰好是当前 router 可见方向，即使其在总范数中占比很小，也可能让样本跨越离散 top-k 边界。

对各向同性随机方向，投影到 1984/2048 维 blind space 后，与原方向的期望夹角约为：

$$
\arccos\sqrt{1984/2048}\approx 10.2^\circ.
$$

因此，4 度是某个具体方向或具体子空间的实测结果，不是由 97% 维数占比必然导出的结论。

但这项批评对“DRPI 相对 static blind”更有力量。DRPI 只从约 1984 维 blind space 中删除 rank 4、8、16 或 32 的 dangerous subspace。若目标方向没有异常对齐该低秩子空间，rank 32 对应的期望夹角也只有约 7.3 度，rank 4 约为 2.6 度。

所以关键问题不是 blind space 还剩多少维，而是：

> 目标方向是否异常集中在下游 route-sensitive 子空间？删除该子空间后，是否在相同目标效果下真正降低了路径分歧或外部损失？

正式实验必须同时报告：

- full、static blind 和 DRPI 之间的夹角；
- dangerous projection 删除的目标方向能量比例；
- 重标定到相同 RMS 后的方向差异；
- 相同目标效果下的 route divergence 和外部损失。

如果 DRPI 与 static blind 的夹角始终很小，目标保留率接近 1，且效果匹配后没有帕累托改善，应直接触发 No-Go，而不应将“没有差异”包装成方法结果。

---

## 2. 关于“BF16 会让程序自己报错停止”

### 判断：基本成立，属于真实 OLMoE 实验前必须修复的阻断项

当前配置使用：

```yaml
dtype: bfloat16
blind_rtol: 1.0e-6
finite_difference_epsilons: [1.0e-4, 3.0e-4, 1.0e-3, 3.0e-3]
```

具体见 [`configs/olmoe_pilot.yaml`](configs/olmoe_pilot.yaml)。

尽管 `blind_basis()` 在 CPU FP32 上计算 SVD，它随后将 basis 转回 router weight 的原 dtype，即 BF16：

```python
return basis.to(device=original_device, dtype=original_dtype)
```

注入时，方向又被强制转为 shared state 的 dtype：

```python
direction = self.injection.direction.to(device=state.device, dtype=state.dtype)
```

相关实现见 [`src/drpi/static_space.py`](src/drpi/static_space.py) 和 [`src/drpi/interventions.py`](src/drpi/interventions.py)。

我用与 OLMoE router 相同的 `64 x 2048` 尺寸做了 BF16 随机数值复现：

- router-null 绝对误差为 `0.804`；
- 相对误差为 `3.47e-4`；
- 两者都会触发当前 `assert_router_blind()`；
- `epsilon=1e-4` 时，2048 个 hidden 分量一个都没有发生可表示变化；
- `epsilon=3e-3` 时，也只有 44 个分量发生变化。

这不能替代真实 OLMoE weight 的最终数值报告，但足以证明当前 dtype 路径与容差设计不相容。

当前 20 个 toy 测试在设置 `PYTHONPATH=src` 后全部通过，但这些测试主要使用 FP32/FP64 小矩阵。因此它们只证明代码在理想高精度条件下满足契约，不能证明真实 BF16 OLMoE 能通过 T1 和 T4。

修复时不应简单放大容差，而应：

1. 让 null-space 构造、方向投影和数值验证保留 FP32 master representation；
2. 分别报告“FP32 理论方向的 null error”和“实际 cast/注入后的 null error”；
3. T1 同时检查 logits 误差、top-k 集合和 gate weight，并使用与 dtype、logit scale 匹配的事前容差；
4. T4 根据 hidden-state scale 和 dtype ULP 自适应选择有限差分步长，先确认 cast 后确实产生可表示变化；
5. 增加 BF16 或实际 backend 数值测试，不再用纯 FP32 toy test 代替真实数值验证。

这些修复通过之前，不应执行或解释 Stage 2 至 Stage 5 的真实模型结果。

---

## 3. 关于“质量指标恒等于 0”

### 判断：对 NLL 和 retain-loss 基线成立，对 output KL 的表述不完全准确

`evaluate_interventions.py` 将干预位置设为最后有效 token：

```python
token = int(batch["attention_mask"].sum().item()) - 1
```

由于 causal Transformer 不允许最后 token 影响此前位置，当前干预只能改变最后位置的 output logits。

`next_token_nll()` 则明确丢弃了最后位置的 logits：

```python
shifted_logits = logits[:, :-1].float().transpose(1, 2)
shifted_labels = input_ids[:, 1:]
```

见 [`src/drpi/metrics.py`](src/drpi/metrics.py)。所以对当前干预位置，`next_token_nll_delta` 在数学上就看不到干预效果。

`build_optimized_baseline.py` 的 `retain_loss` 又使用了同一个 NLL：

```python
base_nll = next_token_nll(baseline_logits, batch["input_ids"])
edit_nll = next_token_nll(edited.output_logits, batch["input_ids"])
constraint = torch.relu(edit_nll - base_nll).mean()
```

因此该 constraint 为 0，`retain_loss` 优化实际退化成 `target_only`。这部分审查意见是正确的。

但是 `output_kl` 没有排除最后位置，因此不必恒为 0。当前问题是代码对全序列 KL 取平均，而只有最后一个位置可能变化：

```python
full_output_kl = float(output_kl(...).mean())
```

这会使 KL 大致按序列长度被稀释。长序列上很小的 KL 不能解释为“模型质量几乎没受影响”。

正确修复应改变评测序列，而不只是修改聚合方式：

1. 在干预 token 后附加预先固定的 continuation；
2. 用 teacher forcing 计算干预位置之后的 token NLL 和 KL；
3. 分别报告 injection-position、post-injection continuation 和 full-sequence 指标；
4. 保留集必须含有实际受干预影响的后续 token；
5. 增加回归测试，验证最后位置干预下 NLL 的无效性，以及加入 continuation 后指标的灵敏性。

---

## 4. 关于“2026 年已经有四篇论文做过”

### 4.1 RARE：实质性方法重叠

RARE：*Decoupling Representation Steering from Expert Routing in Mixture-of-Experts Language Models*，arXiv:2608.21236。

- 原始来源：<https://arxiv.org/abs/2608.21236>
- 将任意 behavioral perturbation 投影到 router matrix 的 null space；
- 明确指出局部 router invariance 不保证下游 router invariance；
- 在选定下游层使用 clean reference state，把传播后的 hidden-state deviation 再次投影到该层 router-null space；
- 在六个开放 MoE 模型以及 harmfulness、truthfulness、factual editing 三类场景中评测；
- arXiv 页面注明被 COLM 2026 Actionable Interpretability Workshop 接收。

因此，原 DRPI 中以下叙事不能再作为主要方法新颖性：

> 单层 router-null 投影只保证当前层，因此需要额外控制下游路由漂移。

RARE 与当前 DRPI 仍有一个可能的差异：

- RARE 需要 clean reference，并在每个 protected layer 做样本级 runtime correction；
- DRPI 试图在 calibration 数据上构造可跨 prompt 复用的下游敏感子空间，然后只在注入层选择一个预防性方向。

这个差异只有在以下证据都成立时才可能构成贡献：

1. 动态敏感子空间能在 held-out prompt 上泛化；
2. 单次方向选择的运行成本显著低于 RARE 的逐层修正；
3. 在相同目标效果下达到可比或更好的 route control；
4. 在外部损失上存在独立价值，而不只是内部 route metric 更小。

否则，当前方法应被视为 RARE 相关的低秩近似或诊断工具，而不是独立的新路由保持方法。

### 4.2 GRIP：数学语言和 margin 约束重叠，但研究变量不同

GRIP：*Algorithm-Agnostic Machine Unlearning for Mixture-of-Experts via Geometric Router Constraints*，arXiv:2601.16905。

- 原始来源：<https://arxiv.org/abs/2601.16905>
- 研究 machine unlearning 中 router parameter update 如何保持 retain-set routing；
- 对已选和未选 expert 之间的 score gap 定义 selection margin；
- 将 router update 投影到保留样本的几何约束集；
- 不属于 inference-time shared hidden-state intervention，也不是本项目的跨层 margin Jacobian。

GRIP 证明“routing margin 和几何投影”本身不新，必须作为强相关工作处理；但把它说成已经完成本项目的 shared-state dynamic intervention，则是过度归纳。

### 4.3 MoEEdit：routing-stable editing 高度相关，但属于参数式知识编辑

MoEEdit：*Efficient and Routing-Stable Knowledge Editing for Mixture-of-Experts LLMs*，arXiv:2602.10965。

- 原始来源：<https://arxiv.org/abs/2602.10965>
- 通过逐 expert null-space projection 重参数化约束 expert parameter update；
- 目标是在参数式 knowledge editing 中保持 router input 和 routing stability；
- 不是单次 forward 中的 shared-state intervention，也没有等价实现当前 DRPI 的下游 margin-gradient subspace。

MoEEdit 大幅压缩了“routing-stable editing”这一宽泛叙事的新颖性空间，但不能直接视为完成了当前项目的 inference-time causal experiment。

### 4.4 OLMoE route-flip 研究：强警告，但不是全称否定

*Detecting a Route Flip Is Easier Than Knowing Whether to Fix It: Causal Route-Mediated Damage in Quantized Mixture-of-Experts*，arXiv:2608.11212。

- 原始来源：<https://arxiv.org/abs/2608.11212>
- 在 OLMoE-1B-7B 的 4-bit KV 量化 pilot 中，router margin 预测 route flip 的 AUC 为 0.772；
- 在所测试的本地、inference-observable router statistics 中，没有找到高于随机水平的 harmful/helpful flip 符号预测器；
- 作者将结论限定为所测特征族的 empirical benefit-detection barrier，没有证明所有上下文、任务和干预中 route switching 都与损失无关。

这篇论文足以否定以下跳跃：

```text
能预测 expert 何时切换
  -> 能判断这次切换是否有害
  -> 减少切换必然减少副作用
```

但它不能直接否定以下更严格的待验证问题：

> 在相同目标效果、干预范数和 baseline margin 下，人为控制 route divergence 是否会对预注册的外部损失产生因果效应？

因此，它不是放弃路由因果研究的理由，而是必须将“路径保持是否有价值”从默认前提降级为待证伪假设的直接证据。

---

## 5. 建议的研究转向

### 5.1 修改中心问题

更稳妥的中心问题是：

> 在控制目标效果后，expert switching 是否因果性地中介非目标损失？单次、分布级的预防性方向选择，是否能以低于 RARE 逐层运行时修正的成本构造有效的 route-controlled 对照？

在这个叙事中，DRPI 首先是尝试控制内部中介变量的实验工具，而不是预先成立的应用方法。

### 5.2 将 RARE 加入首要基线

后续如果继续 DRPI，至少应比较：

- static blind projection；
- RARE-style protected-layer runtime correction；
- DRPI 分布级单次方向；
- 与 DRPI 保留维度相同的 random blind subspace；
- 直接 output-KL 或 retain-loss 约束；
- target-only 优化。

比较必须同时包含目标效果、continuation KL/NLL、保留集损失、额外 forward 次数、clean reference/activation 缓存、运行时间和峰值内存。

### 5.3 增加停止条件

1. **数值 No-Go**：实际 dtype 注入后 T1 或 T4 不能在事前容差下通过，停止下游实验。
2. **区分度 No-Go**：DRPI 与 static blind 在效果匹配后无非平凡差异，不继续方法扩展。
3. **RARE No-Go**：DRPI 不能在运行开销、跨 prompt 泛化或外部损失中至少一项显著优于 RARE-style correction，不将其包装成新控制方法。

---

## 6. 当前证据状态

截至本回复形成时：

- 已核对当前配置、null-space 实现、干预 hook、有限差分和外部损失指标；
- 设置 `PYTHONPATH=src` 后，20 个 toy 测试通过；
- 已用同尺寸 BF16 随机 router 复现精度风险，但这不是真实 OLMoE 数值结果；
- [`results/summary/router_hook_report.json`](results/summary/router_hook_report.json) 仍显示本地模型缓存缺少三个 weight shards，状态为 `blocked_incomplete_cache`；
- 尚未产生真实 OLMoE 泄漏曲线、margin-gradient 报告或效果匹配结果。

因此，目前不存在需要撤回的实验结论。需要撤回或修改的是原有方法新颖性叙事、BF16 实验配置和最后 token 质量评测协议。

## 7. 最终回应

这份审查意见不能用“维数计算不严谨”或“研究任务不完全相同”整体驳回。其中 BF16 数值路径、NLL/retain-loss 退化和 RARE 的方法重叠都是必须正面修正的问题。

同时，也不应接受两个过度推论：

1. blind space 维数大，不等于 full、static blind 和 DRPI 在离散路由功能上必然等价；
2. 已有工作无法从局部 router statistics 判断量化 flip 的好坏，不等于效果匹配的 route-mediated causal experiment 已被全称证伪。

项目仍可继续，但目标应从“提出一个新的路由保持 steering 方法”转为“在 RARE 之后，检验低成本、分布级 route-controlled intervention 是否可行，以及 expert route 是否真正中介外部行为损失”。
