# DRPI 研究意义质疑与论文叙事重构

## 0. 这份文档要解决什么问题

当前研究计划已经把技术边界说得比较严谨：某一层 router 看不见的扰动，并不意味着后续 router 也看不见；DRPI 尝试在当前层的合法自由度中，避开容易导致下游专家切换的方向。

但还有一个比“方法能不能做出来”更根本的问题：

> 即使 DRPI 非常有效，能够在改变目标行为的同时保持更多 expert route，它最终解决了什么重要问题？为什么审稿人应该在意 expert ID 是否保持不变？

如果这个问题回答不清楚，论文可能成为一项技术上漂亮、实验上完整，但价值主张不闭合的工作。本文件专门质疑这一点，并据此重构研究问题、实验顺序和投稿故事。

本文中的判断是对研究计划的修改建议，不是已经得到实验支持的结论。

---

## 1. 先用一句通俗的话说明当前方案

MoE 模型在每一层都会为每个 token 选择少数几个 expert。可以把这些 expert ID 想成模型沿途经过的一串“工位编号”。

当前方案发现了一个精确但局部的操作：我们可以改变某一层的内部状态，同时让这一层仍然选择原来的工位。但是，这个变化会继续经过 expert、残差连接、注意力和后续层，所以走到下一层时，模型可能又换了工位。

DRPI 的目标是：不仅让当前层不换工位，还尽量让后面若干层也不换，同时又实现我们想要的行为改变。

这个问题在数学和机制上很自然。真正需要追问的是：

> 工位编号不变，到底代表什么？它是否真的意味着模型的计算更稳定、行为副作用更小，或者解释更可信？

答案目前未知。因此，“路径保持”不能被默认当作有价值的结果，它本身必须接受实验检验。

---

## 2. 当前计划中最关键的意义断点

当前方案把 expert route 同时当成了三个东西：

1. 被研究的对象：局部 route invariance 能维持多久；
2. 被优化的目标：DRPI 尽量减少下游路径分歧；
3. 方法成功的主要证据：在相同目标效果下，路径分歧更低。

这会产生一个逻辑上的循环：我们先决定路径保持很重要，然后设计方法保持路径，最后用路径保持得更好证明方法有价值。

缺失的一环是：

> 路径分歧是否会导致任何模型外部可感知、用户真正关心或系统真正承担的损失？

例如，它是否导致：

- 非目标能力退化；
- 不相关 token 的预测分布漂移；
- 事实准确率、校准或语言质量下降；
- 安全策略意外改变；
- expert load 失衡、overflow、延迟或吞吐变化；
- 基于 route trace 的解释、监控或审计结论失效。

如果这些关系不存在，那么 expert path 可能只是一个容易测量的内部指标，而不是值得优化的对象。

---

## 3. 审稿人最可能提出的六个质疑

### 3.1 同一路径不等于同一计算

即使两次前向传播选择了完全相同的 expert，这些 expert 接收到的 hidden state 仍然可能不同，产生的激活值和最终输出也可能不同。

因此，保持 expert ID 只能说明离散选择没有变化，不能说明：

- 数值计算没有变化；
- 中间表征没有变化；
- 输出没有副作用；
- 安全性、隐私或可靠性得到保证。

一个简单类比是：两份原料经过同一条生产线，不代表成品一定相同。路线相同，只是机器编号相同；进入机器的数据已经变了。

### 3.2 换路径也不一定意味着功能发生变化

反过来，不同 expert 可能实现近似功能。模型换了 expert，却仍然输出相同答案、保持相同能力和质量。

所以 expert path 对行为可能既不充分，也不必要：

- 路径相同，行为仍然可以不同；
- 路径不同，行为仍然可以相同。

如果论文不主动测量这两种情况，就无法说明 route trace 究竟具有多强的解释力。

### 3.3 为什么不直接约束真正关心的副作用

如果目标是减少输出副作用，更直接的方法是约束：

- 保留集上的输出 KL divergence；
- 非目标任务损失；
- 关键 token 的 log-prob 漂移；
- 隐藏状态或最终 logits 的变化；
- 实际安全、质量或系统指标。

审稿人会问：为什么要绕一圈保持 expert ID？

因此，DRPI 必须与“直接输出保持”或“直接副作用约束”比较。只有在以下至少一种情况下，路径约束才有独立价值：

1. 它比直接输出约束更有效；
2. 它更便宜；
3. 它能跨任务或跨提示更好地泛化；
4. 它提供直接输出约束无法提供的因果解释；
5. 它能控制实际的稀疏系统成本，例如 load、overflow 或延迟。

### 3.4 方法可能只是在优化自己的考试题

DRPI 使用下游 routing margin 的梯度构造方向，最后又用 routing divergence 判断成功。这能证明算法正确优化了设计目标，但不能证明该目标本身重要。

因此，route divergence 应当是中介变量或诊断指标，而不是应用价值的最终证据。真正的主要终点应来自方法构造之外，例如非目标能力、校准、事实准确率或真实系统成本。

### 3.5 Teacher forcing 适合机制研究，但不能自动外推到真实生成

固定 token 序列和 teacher forcing 是比较纵向路径的必要条件。它能回答：在完全相同的 token 上，干预是否改变了后续 expert 选择。

但真实生成中，只要某一步产生了不同 token，后续上下文就分叉了。此时两条路径不再有自然的一一对应关系。

所以第一版 DRPI 更像一个受控实验工具，而不是完整的在线生成控制方案。论文必须明确：

- teacher-forced route 是机制证据；
- free generation 是行为证据；
- 两者不能用同一个路径距离强行连接。

### 3.6 昂贵的逐样本优化可能不是可复用方法

如果 DRPI 对每个 prompt、每个 token 都需要重新保留计算图、计算多个下游 margin 梯度并构造子空间，它更接近单样本优化技巧。

要成为可复用方法，至少需要证明：

- 在 calibration 数据上构造一次动态敏感子空间；
- 在未见过的 prompt 上直接使用；
- 对轻微改写、不同长度和不同 token 位置仍然有效；
- 不为测试样本重新选择 layer、rank、alpha 或约束边界；
- 计算成本相对实际收益可以接受。

---

## 4. 建议重构的中心问题

不建议把论文的最高层问题写成：

> 能否在改变模型行为的同时保持 expert path？

这个写法已经默认 expert path 值得保持。

更有力量、也更科学的中心问题是：

> MoE 暴露的 expert route 到底是不是一种具有因果意义的计算轨迹？当内部干预改变模型行为时，变化来自连续 hidden state 本身，还是由离散 expert switching 所介导？

这一改写带来三个好处。

第一，它不预设路径保持有益。无论路径重要或不重要，都能得到清晰结论。

第二，DRPI 不再只是一个“让指标变小”的算法，而是一个控制变量的因果工具：它帮助我们尽量固定 expert route，再观察行为还能改变多少。

第三，它直接回应 MoE interpretability 中更大的问题：expert route 虽然容易观测，但它是否真的解释模型行为？

---

## 5. DRPI 在新故事中的角色

### 5.1 不把 DRPI 先包装成产品

DRPI 第一阶段最可靠的定位不是“更安全的 steering 方法”，而是：

> 一把尽量固定离散路由变量的因果手术刀。

它帮助构造过去难以得到的对照组：目标行为发生了变化，但 downstream expert path 尽量不变。

有了这个对照，才能进一步判断路径在行为变化中扮演什么角色。

### 5.2 四个最关键的因果象限

| 路径是否改变 | 行为是否改变 | 可得到的结论 |
|---|---|---|
| 否 | 是 | 相同 expert ID 不足以固定行为；route trace 不能完整解释计算 |
| 是 | 否 | expert switching 不是该行为变化的必要条件，可能存在功能冗余路径 |
| 是 | 是 | 路径可能参与行为变化，但相关性本身仍不能证明因果 |
| 否 | 否 | 干预可能太弱、目标方向被约束删除，或该状态维度对行为无效 |

真正有价值的论文不应只展示第三格，而应主动寻找并解释前两格。

### 5.3 什么时候可以升级为应用方法

只有在进一步证实下列因果链后，DRPI 才能被称为减少副作用或提高稳定性的控制方法：

```text
普通干预
  -> 引发额外 expert switching
  -> expert switching 导致可测的非目标损失
  -> DRPI 减少 switching
  -> 在相同目标效果下，真实非目标损失随之下降
```

这条链中任何一箭都不能靠直觉替代实验。

---

## 6. “最后能干什么”的三个层次

### 6.1 第一层：检验 route-based interpretability 是否可信

MoE 的 expert route 是少见的、容易记录的离散内部轨迹。很多解释会自然地把“走了哪些 expert”当成“模型进行了什么计算”。

DRPI 可以检验这种解释的边界：

- 如果路径几乎不变但行为显著改变，route trace 只是粗粒度日志，不能单独承担行为解释；
- 如果路径变化能稳定预测并因果性地带来特定行为变化，route trace 才具有更强的解释价值；
- 如果不同路径产生近似行为，则应研究 expert 的功能冗余和等价路径。

这是最稳的主价值，因为它不要求“路径保持一定好”，只要求能够通过受控干预回答一个重要的解释性问题。

### 6.2 第二层：分解激活干预的作用机制

普通 activation steering 同时改变连续 hidden state 和后续离散路由。如果最终输出变了，我们不知道是哪部分造成的。

DRPI 试图固定其中一个变量，让研究者比较：

- 路径保持的 steering；
- 路径明显变化但目标效果匹配的 steering；
- 不约束路径的普通 steering。

这能将“状态直接作用”和“重路由介导作用”尽量拆开。即便 DRPI 最终不改善输出质量，这个因果分解仍可能是有效贡献。

### 6.3 第三层：在有证据的场景中减少真实副作用

如果实验发现重路由确实带来外部损失，DRPI 才有应用意义。例如：

- 保持目标成功率时，减少非目标任务退化；
- 减少保留集上的输出分布漂移；
- 降低事实错误或校准恶化；
- 稳定 expert load，减少 overflow 或尾延迟；
- 让依赖 route trace 的监控系统不因无关 steering 产生误报。

这些都必须使用真实指标验证，不能由路径 Jaccard 间接推出。

---

## 7. 建议重新排序研究问题

### RQ1：局部 route invariance 的寿命和失效机制是什么？

保持原计划的动态泄漏测量，但目标不只是证明“会泄漏”，而是寻找可压缩、可跨样本复现的规律：

- 泄漏是否主要由 baseline margin 决定；
- 是否存在跨提示稳定的低秩动态敏感子空间；
- layer distance、token 类型、attention mixing 和 expert 非线性分别贡献多少；
- 哪些条件下静态 blind space 本身就足够稳定。

### RQ2：expert route 对行为是否充分、必要或只是相关？

这是新的中心问题。需要系统寻找：

- same-route / different-behavior 样本；
- different-route / same-behavior 样本；
- route divergence 与真实副作用之间的条件相关和因果关系。

### RQ3：DRPI 能否作为有效的 route-controlled intervention？

先回答方法能否在目标效果匹配下改变 route divergence，再检查其是否跨 calibration/test 泛化。

这里的成功只说明 DRPI 是有效的路径控制工具，还不能说明它能减少副作用。

### RQ4：什么时候路径保持具有功能或系统价值？

将路径保持与真实终点连接起来，并明确识别：

- 路径保持有益的任务；
- 路径无关的任务；
- 目标行为必须伴随路径改变的任务；
- 直接输出约束优于 DRPI 的任务。

---

## 8. 实验顺序应当如何倒置

当前计划先测泄漏，再做 DRPI，最后才问路径是否减少副作用。建议在开发复杂方法前，提前验证 route 指标是否有外部效度。

### Stage A：验证 route divergence 是否值得研究

先使用普通 activation steering、随机 blind 方向和 route-visible 方向，扫描多个强度。对每个样本同时记录：

- route divergence；
- 目标行为变化；
- 非目标输出 KL；
- 保留任务损失；
- 语言质量和校准指标；
- 若条件允许，batch-level expert load 与系统指标。

核心问题不是相关系数是否显著，而是：在控制干预范数、目标效果和 baseline margin 后，route divergence 是否仍能解释额外的外部损失。

如果完全不能，DRPI 仍可用于 interpretability 因果实验，但不应包装成副作用控制方法。

### Stage B：完成局部盲性和动态泄漏机制

继续执行原计划的 T1--T4 和 teacher-forced leakage curve。这里仍必须遵守：

- 当前层盲性严格实测；
- 固定 token 序列比较纵向路径；
- 有限差分先于 Jacobian 解释；
- 不把下游近似约束写成全局保证。

### Stage C：构造因果配对干预

目标是尽量形成两组效果匹配的干预：

1. 目标行为相同，但 route divergence 明显不同；
2. route divergence 相近，但目标行为明显不同。

这比单独比较 DRPI 与 static blind 更有信息量，因为它直接检验路径和行为是否可以被解耦。

### Stage D：比较真实副作用，而不是只比较路径指标

在目标效果匹配后，比较：

- DRPI；
- static blind；
- 普通 activation steering；
- 只优化目标的方向；
- 直接输出 KL 保持；
- 直接保留集损失约束；
- 随机等维子空间。

主终点应选一个明确的外部损失。route divergence 作为中介指标，解释为什么某些方法更好或为什么路径其实无关。

### Stage E：检验分布级复用和成本

必须在完全独立的数据上评估 calibration-derived DRPI：

- 未见 prompt；
- 同义改写；
- 不同 token 位置；
- 不同随机种子；
- 至少一个结构不同的 MoE 模型；
- 构造成本、推理额外开销和存储成本。

如果每个测试样本都要重新求梯度，应明确称为 instance-specific intervention，而不是通用控制方向。

---

## 9. 主要终点应该怎样修改

原计划的主要终点是：在相同目标成功率下，DRPI 相对 static blind 降低多少路径分歧，并满足输出质量非劣界限。

这个终点适合验证 route-control 能力，但不足以证明方法价值。建议拆成两层。

### 9.1 机制论文主终点

> 在目标效果和干预范数匹配的因果配对中，改变 downstream expert route 是否会系统性改变预先指定的行为或非目标损失？

这里 DRPI 是实现干预对照的工具，论文结论是 route 的因果解释力及其边界。

### 9.2 方法论文主终点

> 在相同目标效果下，DRPI 是否相对 static blind、普通 steering 和直接输出保持基线，显著降低预先指定的真实非目标损失，并在独立测试提示上保持该优势？

只有这个终点通过，才能把“减少副作用”写成方法贡献。

路径 exact match、Jaccard、routing JS 和首次切换层距仍然重要，但应作为机制或中介指标，而不是最终效用本身。

---

## 10. 任务选择如何优化

### 10.1 模板化实体或颜色替换：保留，但只作为机制单元测试

这类任务因果变量清楚、自动评测容易，适合：

- 验证 target direction；
- 检查效果匹配；
- 构造 same-route / different-behavior 对照；
- 快速判断动态约束是否删除了全部目标信号。

但它不能单独承担应用意义。把 Paris 改成 Lyon，同时少换几个 expert，并不能自然说明实际价值。

### 10.2 选择一个具有明确保留集的行为控制任务

目标行为必须可以客观测量，同时能定义不应该变化的能力。例如：

- 改变输出格式，但保留问题答案正确性；
- 改变语言或语气，但保留事实内容和推理结果；
- 改变情感表达，但保留实体、数值和关系判断；
- 控制某一局部属性，同时在独立能力集上测量退化。

这种任务可以把“副作用”从模糊概念变成明确损失。

### 10.3 系统价值作为高成本扩展，而不是口头动机

如果要声称路径保持改善 MoE 系统稳定性，需要真实测量：

- 每个 expert 的 token load；
- load variance；
- capacity overflow 或 token dropping；
- batch throughput；
- p50/p95/p99 latency；
- 干预构造与执行成本。

单个 token 的路径 Jaccard 不能替代这些指标。

### 10.4 安全任务暂不作为主故事

安全或拒答任务容易受到模板、评测器和模型策略层的影响。除非基本机制和真实副作用链条已经成立，否则它会增加争议，不能替代研究意义的证明。

---

## 11. 必须新增的强基线

现有基线主要围绕 activation 和 route 操作，建议至少新增：

1. **输出 KL 保持基线**：优化目标行为时，直接惩罚保留 token 或保留 prompt 上的输出 KL；
2. **保留集损失基线**：直接约束不希望下降的任务性能；
3. **隐藏状态保持基线**：限制非目标 token 或后续层的 hidden-state drift；
4. **相同计算预算基线**：给目标优化方法相同的梯度、前向次数和校准数据预算；
5. **随机 route-matched 基线**：构造相似 route divergence 但方向来源随机的干预，避免把一般小扰动优势归因于 DRPI。

其中前两项最关键。审稿人需要看到，为什么一个内部路径约束会比直接约束真实目标更值得使用。

---

## 12. 什么样的负结果才真正有论文价值

“router-null 扰动后来会泄漏”本身可能不够惊讶，因为深层网络本来就是非线性的。

负结果要有价值，必须形成更一般、可压缩的机制规律。例如：

- 泄漏几乎完全由少量临界 margin 控制；
- downstream route sensitivity 在 blind coordinates 中呈稳定低秩结构；
- route identity 对行为既非充分也非必要；
- 某类目标方向系统性地落在 route-sensitive space，导致目标改变与路径保持不可兼得；
- route divergence 与非目标损失在控制混杂因素后几乎无关，说明 route trace 不适合作为行为稳定性的代理；
- 静态 blind 在特定层、margin 或模型结构中意外地长期稳定，并能给出可验证条件。

这样的结果能改变研究者如何解释 MoE route。单纯报告若干模型上的平均路径曲线则更像测量工作，故事力度较弱。

---

## 13. 新的 Go/No-Go 决策树

```text
T1--T4 是否通过？
├─ 否：停止，修复 hook、投影或梯度实现。
└─ 是：局部 blind 扰动是否出现结构化的下游泄漏？
   ├─ 否：研究静态稳定成立的条件，不开发复杂 DRPI。
   └─ 是：能否构造目标效果匹配但 route divergence 不同的干预？
      ├─ 否：路径与目标可能不可解耦，报告不可兼得边界。
      └─ 是：route divergence 是否因果性地影响真实外部损失？
         ├─ 否：转为 route trace 因果解释边界论文；不声称减少副作用。
         └─ 是：DRPI 是否优于直接输出/保留集约束？
            ├─ 否：DRPI 是机制工具，不包装成最佳控制方法。
            └─ 是：再检验跨提示复用、第二模型和成本，形成方法论文。
```

这棵树比“DRPI 是否降低路径分歧”更严格，因为它要求每一步都连接到更高层的研究价值。

---

## 14. 推荐的论文故事

### 14.1 最稳的主故事：expert route 是否真的解释行为

一句话版本：

> MoE 暴露了可观测的 expert route，但可观测不等于可解释。我们研究局部 route invariance 在深层网络中的寿命，提出 DRPI 以近似固定下游路径，并用它检验连续状态变化与离散 expert switching 各自如何影响模型行为。

这个故事的主角是一个基础问题：route trace 的因果意义。DRPI 是回答这个问题所需要的新工具。

推荐的三项贡献是：

1. 建立局部 route invariance 的动态失效规律和可预测边界；
2. 提出 route-controlled intervention，用于构造路径与行为解耦的因果对照；
3. 实证刻画 expert route 对行为何时充分、何时必要、何时只是相关，并识别路径保持的功能边界。

### 14.2 有条件升级的应用故事：减少干预副作用

只有在真实外部终点显著改善时，才能使用下面的故事：

> Activation interventions in MoEs alter both continuous states and discrete expert computation, making collateral effects difficult to control. DRPI preserves vulnerable downstream routing boundaries and reduces measured non-target degradation at matched target effect.

这个版本必须同时击败 static blind、普通 steering 和直接输出保持基线。

### 14.3 不建议作为主故事的说法

- “我们在不改变计算过程的情况下修改模型内容”；
- “路径不变保证没有副作用”；
- “router-blind space 是内容空间”；
- “DRPI 是一种持久知识编辑方法”；
- “保持 expert ID 自动提高安全性或隐私”；
- “路径保持更好，所以方法更好”。

---

## 15. 可直接用于 Introduction 的通俗论证骨架

第一段：MoE 给研究者提供了一条罕见的离散内部轨迹：每个 token 在每层选择哪些 expert。人们很容易把这条轨迹理解成模型的“计算路线”。但我们还不知道，这条路线究竟只是可观测日志，还是对模型行为具有因果解释力。

第二段：单层 router 的零空间提供了一个精确控制条件。我们可以改变 router 和 expert 共同接收的状态，同时让当前层的 route logits 不变。然而该变化会继续经过 expert、残差连接、注意力和归一化，所以局部不变性未必能在深层网络中维持。

第三段：这个缺口不只是一项数学细节。如果相同 route 下行为仍能显著改变，那么 route trace 不能完整解释模型计算；如果不同 route 可以产生相同行为，那么 expert switching 也不是该行为的必要条件。要判断 route 的因果意义，需要能够主动改变状态，同时尽量控制后续 route。

第四段：我们先测量局部 route invariance 的寿命，并检验 margin 和 Jacobian 是否能预测其失效。随后提出 DRPI：只在当前 router 的 blind coordinates 中操作，并避开会推动下游 expert 越过选择边界的敏感方向。

第五段：我们不以“路径分歧下降”本身证明价值，而是构造目标效果匹配、路径变化程度不同的干预，检验 route 是否介导非目标行为和系统损失，并与直接输出保持约束比较。

第六段：最终结论应由实验决定。可能的结论包括：route 对部分任务有真实因果作用；route 只是一种粗粒度代理；或者目标行为与路径保持在某些任务中不可兼得。三者都比预设“路径保持总是有益”更可信。

---

## 16. 英文摘要式故事草案

下面是论证骨架，不代表实验结果。方括号内容必须由实际数据替换。

```text
Mixture-of-Experts models expose a discrete trace of computation through the
experts selected for each token. This trace is easy to observe, but its causal
meaning is unclear: does preserving expert identity preserve anything that
matters about model behavior, and are behavioral changes mediated by expert
switching or by continuous state transformations within the same route?

We study this question through interventions that are exactly invisible to the
router at an injection layer. We show that this local invariance has a finite
and structured lifetime across depth [only if observed], and test whether
downstream routing margins and their local sensitivities predict when it fails.
We then introduce Dynamic Route-Preserving Intervention (DRPI), which operates
within the exact local router-null space while suppressing directions that are
sensitive to vulnerable downstream routing boundaries.

Rather than treating route preservation as an end in itself, we use DRPI to
construct effect-matched interventions with different amounts of downstream
rerouting. This lets us test when expert routes mediate behavioral and
collateral changes, when behavior changes despite a stable route, and when
different routes remain functionally equivalent. Across [models and tasks],
our results [state only observed findings], establishing both the explanatory
value and the limits of expert routes as traces of MoE computation.
```

---

## 17. 最小可执行研究版本

在时间有限的情况下，建议把首轮研究压缩为以下五步：

1. 完成模型检查和 T1--T4，确认干预与梯度结构有效；
2. 在一个模型上得到可信的静态 blind 泄漏曲线和 margin-conditioned 规律；
3. 用一个清晰的格式或属性控制任务，构造效果匹配但路径分歧不同的干预对；
4. 同时测量 route divergence、保留集输出 KL 和非目标任务损失；
5. 比较 DRPI、static blind、普通 steering 和直接 KL 保持基线。

这一版本至少能够回答：route 是否能与行为解耦，以及 route preservation 是否比直接输出保持多提供了什么。

在这五步完成前，不建议扩展跨语言、安全任务、第三个模型或大规模自由生成。

---

## 18. 最终判断

当前研究计划有一个可靠的技术核心：局部 router-null 保证与深层网络动力学之间存在需要实测的缺口，DRPI 为控制这个缺口提供了明确方法。

但论文意义不能停在“我们更好地保持了 expert path”。expert path 是内部变量，不是天然的价值指标。真正应该回答的是：

> expert route 究竟解释了什么？固定它以后，行为还能改变多少？主动改变它又会额外造成什么？

因此，最优叙事不是把 DRPI 直接包装成更安全、更稳定的干预方法，而是先把它定位为研究 MoE 路由因果作用的工具。若实验进一步证明 route switching 导致真实副作用，而且 DRPI 比直接输出约束更有效或更可复用，再将它升级为应用方法。

这样，无论实验结果是路径重要、路径不重要，还是某些目标与路径保持不可兼得，论文都能给出清晰、可证伪且对 MoE interpretability 有意义的结论。
