# 研究方案：从局部零空间到动态路由保持干预

## 面向 ICLR 2027 的 MoE 多层因果控制与状态干预方法

> **一句话概括**：MoE 路由器的零空间只能保证干预不改变当前层路由，不能保证后续路径不变。本研究首先系统刻画这种“路由不可见性”如何沿网络深度泄漏，再利用下游路由 margin 的 Jacobian 构造动态路由保持干预，在改变模型内部内容表征或输出行为的同时，显式约束多层专家路径。

---

## 0. 执行摘要

现有工作将 MoE 某层路由器输入分解为路由可见分量与路由不可见分量。若第 $l$ 层路由 logits 为

$$
z_l = R_l h_l+b_l,
$$

则位于 $R_l$ 零空间的扰动 $\delta$ 满足 $R_l\delta=0$，因此不会改变该层的路由 logits。这个结论是严格的，但只是**层内、瞬时和局部**的：扰动仍会经过专家网络、残差连接和后续层，因而可能在第 $l+1$ 层重新变得路由可见。

本研究不预设局部正交性能够自然延续到最终行为，而是研究三个更严格的问题：

1. 局部路由不可见扰动经过多少层后会显著改变专家路径？
2. 这种下游泄漏能否由路由 margin、网络 Jacobian 和干预位置预测？
3. 能否构造一个多层动态约束空间，使干预在改变目标内容或行为的同时，尽可能保持后续专家路径？

论文的目标不是证明“路由可见=控制、不可见=内容”，而是建立一套可检验的**动态路由因果框架**。即使最终无法实现强内容编辑，关于局部零空间并非动态不变量的系统性结论仍然具有独立研究价值。

---

## 1. 研究动机

### 1.1 已知的局部保证

对路由 logits 去除不影响 top-k 和 softmax 的公共平移方向，定义中心化矩阵

$$
C=I-\frac{1}{E}\mathbf{1}\mathbf{1}^{\top},
$$

则当前层真正与相对路由决策有关的线性映射是 $CR_l$。相应的静态路由盲空间为

$$
\mathcal N_l^{\mathrm{static}}=\ker(CR_l).
$$

这一写法比直接使用 $\ker(R_l)$ 更接近最小的路由不变空间，因为给全部专家 logits 增加同一个常数不会改变路由结果。

### 1.2 尚未解决的动态问题

设在第 $l$ 层注入 $\delta_l\in\mathcal N_l^{\mathrm{static}}$。尽管当前层路由不变，后续第 $k$ 层的 logits 一般满足

$$
z_k(h_l+\delta_l)-z_k(h_l)\neq 0,\qquad k>l.
$$

原因是专家计算和后续 Transformer 模块是非线性的，静态零空间通常不会被网络动力学保持。现有“控制/内容通道”的解释尚未回答：

- 路由不变性能够维持多深；
- 哪些层、token 和专家边界最容易发生泄漏；
- 路由不变与语义改变之间是否存在可利用的自由度；
- 多层路由约束是否比简单静态投影带来真实优势。

这正是本方案的研究空白。

---

## 2. 核心研究问题与假设

### RQ1：局部零空间是否是动态不变量？

**H1a（预计成立）**：静态 blind 扰动对当前层路由严格不可见，但通常会在后续若干层产生可测的 centered-logit 漂移和 top-k 路径分歧。

**H1b（开放假设）**：泄漏速度具有结构性，而不是纯随机现象；它可由下游 Jacobian 范数、当前路由 margin、干预层深度和 token 类型共同预测。

### RQ2：是否存在非平凡的动态路由保持子空间？

**H2（开放假设）**：对有限的下游层和固定输入分布，多个路由约束的近似零空间仍保留足够自由度，可以承载非零的内容或行为干预。

### RQ3：动态约束是否优于静态投影？

**H3（核心方法假设）**：在匹配目标效果、干预范数和计算预算后，动态路由保持干预能比静态 blind 投影、普通 activation steering 和随机等维投影获得更好的“目标改变—路径漂移—输出副作用”帕累托前沿。

### RQ4：路径保持是否真的具有功能价值？

**H4（必须验证而非默认）**：在部分任务中，保持原始专家路径可减少非目标行为漂移；但在另一些任务中，改变路径可能是完成目标所必需的。论文应明确给出二者的适用边界。

---

## 3. 预期贡献

1. **动态泄漏图谱**：系统测量局部 router-null 扰动在后续层重新变得路由可见的速度、位置和条件。
2. **可预测机制**：用路由 margin 与跨层 Jacobian 解释何时发生专家切换，而不仅报告路径相关性。
3. **动态路由保持干预 DRPI**：把多层路径保持写成明确的线性化约束或优化问题，而不是依赖单层投影。
4. **严格的因果评测协议**：区分当前层路由、同一 token 的后续层路径和自由生成路径，并提供公平的效应匹配基线。
5. **边界性结论**：识别何时路径保持有助于控制副作用，何时路径本身必须改变。负结果也属于核心结论，而不是预案中的降级故事。

在完成全面文献检索前，论文不应使用“第一个”之类的排他性新颖性表述。

---

## 4. 严格问题定义

### 4.1 干预位置

干预必须定义在**路由器与专家实际共同接收的张量**上，并记录它位于 RMSNorm/LayerNorm 之前还是之后。若在归一化之前加入扰动，非线性归一化可能破坏 $R_l\delta=0$；若路由器和专家使用不同输入，则必须分别定义 router-only 与 shared-state intervention。

主实验采用 shared-state intervention，因为只有这种设置才能讨论“改变专家处理的内容而保持路由”。router-only logit 修改只作为强基线。

### 4.2 三种不同的路由保持

必须区分：

1. **即时保持**：第 $l$ 层当前 token 的路由不变；静态零空间对此提供严格保证。
2. **纵向保持**：同一 token 从第 $l+1$ 层到第 $L$ 层的专家路径不变；这是主要研究对象。
3. **生成保持**：自由生成后续 token 的完整路由轨迹可比较；一旦生成文本不同，token 不再对齐，因此不能直接声称路径保持。

纵向路径实验必须使用相同输入和 teacher forcing，在相同 token 位置上比较。自由生成只评估行为和质量，不把未对齐路径的 Hamming 距离当作主要证据。

### 4.3 路由 margin

对 top-k 已选专家 $i$ 与未选专家 $j$，定义边界 margin：

$$
m_{k,t,i,j}=z_{k,t,i}-z_{k,t,j}.
$$

保持专家集合只要求所有活跃边界满足 $m_{k,t,i,j}>0$，不需要让全部 logits 完全相同。这比简单约束 $\Delta z=0$ 更宽松，也保留更多可干预自由度。

---

## 5. 方法一：动态泄漏测量

### 5.1 静态 blind 干预

在第 $l$ 层构造

$$
\delta_l^{\mathrm{static}}=P_{\mathcal N_l}d,
$$

其中 $d$ 可以是随机方向、对比激活方向或由目标输出梯度得到的方向。对不同范数、层、token 位置和方向重复实验。

### 5.2 路由生存曲线

定义深度偏移 $\Delta=k-l$ 下的路径生存率：

$$
S_l(\Delta)=\Pr[\operatorname{TopK}(z_{l+\Delta}^{\delta})=
\operatorname{TopK}(z_{l+\Delta}^{0})].
$$

同时报告：

- centered-logit cosine/L2 漂移；
- routing distribution 的 Jensen–Shannon divergence；
- top-k 集合 Jaccard 与顺序敏感 Hamming；
- 首次专家切换发生的层距；
- 路径分歧与原始 margin 的关系。

### 5.3 Jacobian 泄漏预测

对下游 centered logits 或活跃 margin 定义

$$
J_{l\rightarrow k,t}=
\frac{\partial (Cz_{k,t})}{\partial h_l}.
$$

在线性近似下：

$$
\Delta z_{k,t}\approx J_{l\rightarrow k,t}\delta_l.
$$

对专家边界则有

$$
\Delta m_a\approx g_a^{\top}\delta_l,
$$

其中 $a=(k,t,i,j)$，$g_a=\nabla_{h_l}m_a$。检验 $m_a+g_a^{\top}\delta_l$ 是否能预测实际专家切换。主要指标是 AUROC、校准误差和跨样本泛化，而不是只报告相关系数。

---

## 6. 方法二：动态路由保持干预（DRPI）

### 6.1 低秩动态约束空间

令 $B_l$ 的列构成当前层静态盲空间 $\mathcal N_l^{\mathrm{static}}$ 的正交基。所有候选干预都参数化为

$$
\delta_l=B_lu,
$$

从而无论怎样优化 $u$，当前层相对路由 logits 都严格不变。在这个参数域内，收集一组对任务重要的下游层、token 位置和接近 top-k 边界的 margin 梯度，堆叠为

$$
A_l=
\begin{bmatrix}
g_1^{\top}\\
g_2^{\top}\\
\vdots\\
g_M^{\top}
\end{bmatrix}.
$$

真正作用于静态盲空间坐标的下游敏感矩阵是

$$
\widetilde A_l=A_lB_l.
$$

通过 randomized SVD、JVP/VJP 或低秩梯度草图近似 $\widetilde A_l$ 的主行空间，得到正交基 $Q_l$。最简单的动态投影为

$$
\delta_l^{\mathrm{DRPI}}
=B_l(I-Q_lQ_l^{\top})B_l^{\top}d.
$$

这里的 $Q_l$ 描述静态盲空间内部“会显著改变下游路由边界的方向”，因此其正交补空间是分布条件下的近似动态盲空间。这个两级构造保证：当前层盲性是严格的，下游盲性是由数据和线性化共同定义的近似性质。

### 6.2 软约束版本

硬投影可能删除过多目标信号。保持 $\delta=B_lu$ 的参数化，采用正则化优化：

$$
\max_{u}\quad (B_l^{\top}c)^{\top}u
-\gamma\|u\|_2^2
-\lambda\|A_lB_lu\|_2^2,
$$

其中 $c$ 是目标内容或行为方向。通过改变 $\lambda$ 获得目标效果与路径漂移的帕累托曲线。

### 6.3 Margin 约束版本

对活跃的下游路由边界直接求解，同时继续用 $\delta=B_lu$ 保证当前层路由不变：

$$
\begin{aligned}
\max_{u}\quad & c^{\top}B_lu-\gamma\|u\|_2^2\\
\text{s.t.}\quad & m_a+g_a^{\top}B_lu\ge \epsilon,
\quad a=1,\ldots,M.
\end{aligned}
$$

只约束最接近决策边界的专家对，以控制计算量。若非线性验证发现约束被破坏，则使用逐次线性化重新计算梯度，而不是声称一次 Jacobian 近似具有全局保证。

### 6.4 分布级而非样本级版本

样本专用 Jacobian 容易过拟合。训练集上聚合低秩路由敏感方向，在验证集选择秩和正则系数，在完全独立的测试提示上评估。这一区分决定方法是“单样本优化技巧”还是可复用干预方法。

---

## 7. 实验任务

### 7.1 机制任务：随机与结构化扰动

目的不是追求应用效果，而是建立泄漏规律。

- 随机静态 null-space 方向；
- 随机 route-visible 方向；
- 相同维度的随机子空间方向；
- 不同范数、层和 token 类型；
- 高 margin 与低 margin 样本分层。

该任务用于验证 H1，并为后续方法选择约束层和 rank。

### 7.2 对齐的反事实内容替换

构造句法结构相同、只改变一个实体或属性的 A/B 输入对，例如人物、地点、颜色或数值替换。提取 B−A 的目标方向，在 A 的对应 token 位置干预。

主要测量：

- 目标实体/属性的输出概率是否按预期改变；
- 同一固定序列上的下游专家路径是否保持；
- 非目标 token 的预测分布与语言质量是否漂移。

该任务只称为“反事实状态编辑”，不宣称为持久知识编辑。

### 7.3 可控行为任务

选择两个机制不同的属性，避免只在一个容易任务上得出结论：

1. 一个局部、可客观判定的属性，如输出语言或格式；
2. 一个语义属性，如情感极性或主题倾向。

拒答/越狱不作为首轮主任务，因为安全策略、多模板敏感性和评测器偏差会掩盖路由机制；可在方法成立后作为扩展。

### 7.4 跨语言实验的降级定位

跨语言不再作为理论必然结论，而作为条件性外推：先验证英文和中文中动态敏感子空间的主角度、margin 梯度相似度和路径分布是否对齐，再测试方向迁移。若几何不对齐，不继续声称“零成本迁移”。

---

## 8. 必须包含的强基线

1. 普通全残差流 activation steering；
2. 当前层静态 blind 投影；
3. 当前层 route-visible 投影；
4. 与 DRPI 相同维度的随机子空间投影；
5. 直接 router-logit bias；
6. 最小 margin crossing 干预；
7. 强制 top-k 专家替换或 expert masking；
8. 专家输出缩放；
9. 只优化目标、不加入路径约束的梯度干预；
10. 在可行时加入 dense 模型对照，区分一般 activation control 与 MoE 特有收益。

最小 margin crossing 基线尤其重要。把专家 $i$ 切换为 $j$ 的线性最小范数扰动为

$$
\delta h=
\frac{m+\epsilon}{\|r_j-r_i\|_2^2}(r_j-r_i),
$$

它可以非常容易地改变当前路由。新方法必须证明优势来自多层路径约束，而不只是“能够改变专家选择”。

---

## 9. 公平比较与统计设计

### 9.1 三套匹配协议

所有核心方法至少在以下条件下分别比较：

- **范数匹配**：相同 $\|\delta\|_2$；
- **目标效果匹配**：达到相同目标属性变化后比较路径漂移和副作用；
- **路由效果匹配**：达到相同 routing JS、top-k 切换率或路径保持率后比较目标效果。

层、token 位置、方向提取数据和超参数预算也必须相同。$\lambda$、rank、干预层与强度只在验证集选择。

### 9.2 主要终点

预先指定一个主要终点，避免从大量指标中挑选正面结果：

> 在相同目标成功率下，DRPI 相对静态 blind 投影降低的下游路径分歧，并同时满足预设的输出质量非劣界限。

### 9.3 统计方法

- 使用逐提示配对比较；
- 报告效应量和 95% bootstrap 置信区间；
- 对主要终点使用配对置换检验；
- 多层、多任务探索性检验使用 Holm 校正；
- 方向提取至少使用三个独立样本划分或随机种子；
- 同时报告均值、分位数和失败案例，不只报告最优层结果。

“统计显著”不能代替实际效果。应预先设置最小实际改善阈值，例如在目标效果相同条件下，下游路径分歧至少相对下降 20%，且输出质量下降不超过预设非劣界限。具体阈值由小规模 pilot 后冻结。

---

## 10. 模型与计算范围

### 最小可行研究

- 主模型：OLMoE-1B-7B，完成全部机制实验和方法消融；
- 验证模型：选择一个架构不同、规模可承受的开源 MoE；
- 第三个大模型只重复最关键的一个图表，不复制全部网格。

### 完整研究

在 3 个具有不同专家数、top-k、共享专家或归一化结构的模型上验证。五个大模型全覆盖不是必要条件；模型数量不能替代严谨的机制证据。

每个模型必须记录：

- router 输入的准确 hook 点；
- 路由器是否有 bias、归一化、共享专家和额外 gating；
- $CR_l$ 的数值秩及阈值；
- top-k margin 分布；
- 干预是否同时影响 router 与 expert 输入。

---

## 11. 关键图表

1. **局部盲性泄漏图**：横轴为下游层距，纵轴为路径生存率，不同曲线对应干预范数和层。
2. **margin–Jacobian 预测图**：预测切换概率与实际专家切换的校准曲线。
3. **三目标帕累托图**：目标成功率、路径漂移、输出副作用。
4. **动态子空间谱图**：约束 rank 增加时剩余自由度与目标信号保留率。
5. **边界条件图**：哪些层、任务和 margin 区域适合路径保持，哪些不适合。
6. **失败案例图**：线性化约束满足但实际非线性路径发生切换的实例。

---

## 12. 可证伪标准与转向条件

### F1：不存在有意义的动态泄漏

若静态 blind 扰动在合理范数下本来就能保持大部分后续路径，则复杂 DRPI 没有必要。研究应转为给出静态方法成立的条件与理论解释，而不是强行引入新算法。

### F2：Jacobian 无法预测路由切换

若线性化预测在小扰动下仍接近随机，则当前方法基础不成立。需研究归一化、注意力耦合或二阶项，而不能继续宣称动态约束有效。

### F3：动态约束删除了全部目标信号

若目标方向几乎完全落在下游路由敏感空间中，说明“改变目标但保持路径”在该任务上不可兼得。这是有价值的因果结论，应报告任务依赖性。

### F4：DRPI 不优于简单基线

若在效果匹配后，DRPI 不优于静态投影或直接目标优化，则不应包装成新的控制方法。论文可转向“局部盲性如何失效”的机制分析，但必须有跨层预测和系统边界，而不只是负实验合集。

---

## 13. 快速 Go/No-Go 实验

在投入大规模评测前，用一个模型完成以下 3–5 天实验：

1. 精确定位 OLMoE 的 router/expert 共用输入并验证当前层 logits 不变；
2. 在 6–8 个代表层、多个范数下绘制静态 blind 扰动的路径生存曲线；
3. 检验 margin 与一阶 Jacobian 是否能预测下一层和后续层专家切换；
4. 在一个对齐实体替换任务上比较 full、static blind 和初版 DRPI；
5. 采用范数匹配和目标效果匹配，不调参挑最好层。

**继续条件**：同时观察到可重复的动态泄漏、可接受的 Jacobian 预测能力，以及 DRPI 相对静态投影的非平凡帕累托改善。

若只有前两项成立，则写机制论文；若三项均不成立，应停止该方向，而不是扩大模型数量寻找偶然正结果。

---

## 14. 时间计划

### ICLR 2027 极限压缩版

以 2026 年 8 月 30 日为起点，官方摘要截止时间为 9 月 18 日 AoE，正文截止时间为 9 月 25 日 AoE。只有在代码环境和算力已经具备时，才可能尝试：

- 第 1–3 天：hook、数值验证、固定序列 route capture；
- 第 4–7 天：OLMoE 动态泄漏图谱与 Go/No-Go；
- 第 8–13 天：DRPI 初版、强基线、一个主任务；
- 第 14–18 天：第二任务或第二模型、冻结实验设计、提交摘要；
- 第 19–22 天：完整测试集、统计分析、消融；
- 第 23–26 天：写作、复查、提交。

这是高风险时间表，不足以支持五个模型、跨语言和安全应用同时展开。ICLR 版本应只讲“动态泄漏 + DRPI + 两个任务/两个模型”的单一故事。

### 完整版

合理周期为 8–10 周：

- 2 周：机制图谱和预测模型；
- 2–3 周：DRPI 算法与效率优化；
- 2 周：任务、基线和统计评测；
- 1–2 周：跨模型复现；
- 1 周：写作和复核。

---

## 15. 投稿故事

> MoE 路由器的零空间提供了严格但局部的路由不变性。我们检验这种不变性是否会被深层网络动力学保持，并系统刻画局部 blind 扰动如何在后续层重新影响专家选择。基于下游路由 margin 的 Jacobian，我们提出动态路由保持干预 DRPI，在不假设“控制/内容”天然分离的前提下，直接优化目标改变与多层路径保持之间的权衡。实验不仅比较最终行为，还在效果匹配条件下检验路径保持是否真正减少副作用，并明确给出该目标可行与不可行的边界。

---

## 16. 最终判断

这个重构方向比“在 $h_{vis}$ 上 steering、在 $h_{blind}$ 上编辑”的原始方案更难，但研究逻辑更稳固：

- 不把当前层代数保证夸大为下游行为保证；
- 简单投影成为必须击败的基线，而不是论文贡献本身；
- 正结果可以形成动态约束方法；
- 负结果可以形成关于 MoE 路由动力学的机制结论；
- 所有核心主张都有明确的证伪实验。

真正决定论文价值的不是能否轻易改变一次专家选择，而是能否解释并控制干预在多层网络中的传播。

---

## 参考起点

1. Charles Ye, Bo Yuan, Lee Sharkey. *Polysemantic Experts, Monosemantic Paths: Routing as Control in MoEs*. arXiv:2604.17837, 2026. <https://arxiv.org/abs/2604.17837>
2. ICLR 2027 Dates and Deadlines. <https://iclr.cc/Conferences/2027/Dates>

完整投稿前需对 activation steering、causal tracing、route editing、Jacobian subspace intervention、MoE interpretability 和 route-preserving optimization 做一次系统文献检索，并据此收紧新颖性表述。
