# DRPI 项目运行手册

本文说明如何从当前仓库状态开始，依次完成环境检查、模型检查、静态 blind 泄漏、margin/Jacobian 验证、DRPI 构建和效果匹配评测。

> 科学边界：必须按阶段推进。Stage 0 或 T1-T4 未通过时，不得运行或解释后续实验。teacher forcing 下的纵向路径证据不能外推为自由生成路径保证，路径保持也不是输出无副作用的证明。

## 0. 当前状态

- 工程代码、配置、400 条反事实数据和 toy MoE 测试已经存在。
- toy 测试通过只说明实现契约正常，不说明 OLMoE 上存在动态泄漏或 DRPI 有效。
- 当前 `results/summary/router_hook_report.json` 为 `blocked_incomplete_cache`：模型索引存在，但三个 safetensors 分片尚未完整下载。
- MPS 需要在可访问 Metal 的非沙箱终端中运行。
- 仓库已经初始化 Git，但尚无 commit。正式实验前建议先形成一个可追溯的代码 commit，否则结果中的 `code_commit` 会是 `null`。

## 1. 进入仓库并设置环境

每次打开新终端先执行：

```bash
cd "/Users/trickeye/Library/Mobile Documents/iCloud~md~obsidian/Documents/ProjectBudapest/ProjectSF-ICLR-MoE-DRPI"
export PYTHONPATH=src
export MPLCONFIGDIR=/tmp/drpi-matplotlib
mkdir -p "$MPLCONFIGDIR"
```

确认 Python、PyTorch、Transformers 和 MPS：

```bash
conda run -n moe-steering python -c '
import torch, transformers
print("torch:", torch.__version__)
print("transformers:", transformers.__version__)
print("mps built:", torch.backends.mps.is_built())
print("mps available:", torch.backends.mps.is_available())
x = torch.ones(4, device="mps")
print("MPS tensor sum:", x.sum().item())
'
```

预期至少看到 `mps built: True`、`mps available: True` 和 `MPS tensor sum: 4.0`。若 `mps available` 为 `False`，先确认当前命令是否运行在受限沙箱中，不要立即修改项目配置。

检查依赖和工程测试：

```bash
conda run -n moe-steering pip check
conda run -n moe-steering pytest -q
```

当前基线预期为 `20 passed`。任何测试失败都应先修复，不能直接进入模型实验。

## 2. 准备固定 revision 的 OLMoE 权重

配置固定在 `configs/olmoe_pilot.yaml`：

```text
model: allenai/OLMoE-1B-7B-0924
revision: 6d84c48581ece794365f2b8e9cfb043c68ade9c5
device: mps
dtype: bfloat16
local_files_only: true
```

下载三个权重分片：

```bash
HF_HUB_DISABLE_XET=1 conda run -n moe-steering hf download \
  allenai/OLMoE-1B-7B-0924 \
  model-00001-of-00003.safetensors \
  model-00002-of-00003.safetensors \
  model-00003-of-00003.safetensors \
  --revision 6d84c48581ece794365f2b8e9cfb043c68ade9c5 \
  --max-workers 1
```

本机此前访问 `huggingface.co` 发生过连接超时。若失败，保留 `.incomplete` 文件并稍后重试；不要手工把 `.incomplete` 政名为正式分片。也可以在网络通畅的机器下载同一 revision，再完整复制 Hugging Face snapshot 和 blobs。

下载完成后，用项目自己的检查器确认，而不是只相信下载命令的退出码：

```bash
conda run -n moe-steering python -c '
from drpi.loading import check_weight_cache
r = check_weight_cache(
    "allenai/OLMoE-1B-7B-0924",
    "6d84c48581ece794365f2b8e9cfb043c68ade9c5",
)
print(r)
assert r.complete, r.missing_shards
'
```

只有 `complete=True` 才进入 Stage 0。

## 3. Stage 0：模型、hook 和 MPS 检查

执行：

```bash
conda run -n moe-steering python scripts/inspect_model.py \
  --config configs/olmoe_pilot.yaml \
  --out results/summary/router_hook_report.json
```

检查报告：

```bash
conda run -n moe-steering python -c '
import json
r = json.load(open("results/summary/router_hook_report.json"))
print("status:", r["status"])
print("shared state:", r.get("shared_state_verified"))
print("manual router error:", r.get("manual_router_max_abs_error"))
print("gradient smoke:", r.get("gradient_smoke"))
'
```

继续条件：

- `status == "passed"`；
- `shared_state_verified == true`；
- 手工 router 前向与捕获 logits 的误差在报告容差内；
- gradient smoke 为 `passed` 且梯度 finite；
- 报告确认 MoE block 输入同时送入 gate 和 experts，且 hook 位于 `post_attention_layernorm` 之后。

若失败：停止 Stage 1 以后所有实验。先修复 adapter、hook、dtype 或模型缓存，不要通过放宽容差继续。

## 4. Stage 1：T1-T3 最小真实模型 smoke test

Stage 1 的通用数学、hook 生命周期和 locality 已由 toy 测试覆盖。真实模型的 T1 会在泄漏脚本每次运行时强制检查：注入层目标 token 的 logits 必须数值一致且 top-k 完全相同，否则脚本立即终止。

先只跑 1 条 calibration prompt：

```bash
conda run -n moe-steering python scripts/leakage_curve.py \
  --config configs/olmoe_pilot.yaml \
  --split calibration \
  --limit 1 \
  --out results/raw/leakage_smoke.jsonl
```

检查原始记录存在且没有 NaN：

```bash
conda run -n moe-steering python -c '
import json, math
rows = [json.loads(x) for x in open("results/raw/leakage_smoke.jsonl") if x.strip()]
assert rows
for row in rows:
    assert math.isfinite(row["router_null_absolute"])
    assert math.isfinite(row["router_null_relative"])
print("rows:", len(rows))
print("max null error:", max(x["router_null_absolute"] for x in rows))
'
```

注意：原始 JSONL 是追加式文件。重复运行同一 smoke 命令会根据键跳过已完成项；正式实验应使用新的、语义清楚的文件名，不能覆盖已有原始结果。

## 5. Stage 2：静态 blind 动态泄漏

### 5.1 小规模 pilot

先把 `--limit` 提升到 8：

```bash
conda run -n moe-steering python scripts/leakage_curve.py \
  --config configs/olmoe_pilot.yaml \
  --split calibration \
  --limit 8 \
  --out results/raw/leakage_pilot.jsonl

conda run -n moe-steering python scripts/plot_leakage.py \
  --input results/raw/leakage_pilot.jsonl \
  --out results/summary/leakage_pilot.png
```

打开 `results/summary/leakage_pilot.png`，重点看：

- 路径生存率是否随 layer distance 或 alpha 呈稳定变化；
- 变化是否不只来自一个 seed、一个注入层或极端 margin；
- 注入层 `horizon=0` 是否仍满足即时盲性。

### 5.2 正式 calibration 泄漏实验

pilot 正常后去掉 `--limit`：

```bash
conda run -n moe-steering python scripts/leakage_curve.py \
  --config configs/olmoe_pilot.yaml \
  --split calibration \
  --out results/raw/leakage_calibration.jsonl

conda run -n moe-steering python scripts/plot_leakage.py \
  --input results/raw/leakage_calibration.jsonl \
  --out results/summary/static_blind_leakage.png
```

这一步网格较大且会运行很久。脚本逐条 fsync 并支持断点续跑；不要同时启动两个进程写同一个 JSONL。

Go/No-Go：只有泄漏超过数值噪声、至少三个方向种子复现，并随强度、层距或 baseline margin 呈结构关系，才进入 Stage 3。若没有结构化泄漏，停止 DRPI，转向研究静态 blind 稳定成立的条件。

## 6. Stage 3A：T4 margin 梯度有限差分

先选一个中间注入层做代码 smoke test，例如 layer 6 到 layer 7：

```bash
conda run -n moe-steering python scripts/verify_margin_gradient.py \
  --config configs/olmoe_pilot.yaml \
  --layer 6 \
  --downstream-layer 7 \
  --split calibration \
  --limit 8 \
  --out results/summary/margin_gradient_l6_l7.json
```

查看：

```bash
conda run -n moe-steering python -c '
import json
r = json.load(open("results/summary/margin_gradient_l6_l7.json"))
print("passed:", r["passed"])
print("relative error pass rate:", r["relative_error_pass_rate"])
print("sign pass rate:", r["sign_pass_rate"])
'
```

必须满足：至少 90% 边界相对误差不超过 10%，符号一致率至少 95%。不通过时依次检查 hook、epsilon、alpha 和 horizon；T4 不通过不得解释 Jacobian 预测结果。

## 7. Stage 3B：held-out margin/Jacobian 预测

先在 calibration 生成预测记录：

```bash
conda run -n moe-steering python scripts/evaluate_margin_prediction.py \
  --config configs/olmoe_pilot.yaml \
  --layer 6 \
  --downstream-layer 7 \
  --split calibration \
  --out results/raw/margin_prediction_calibration.jsonl
```

在查看 test 前冻结 injection layer、downstream layer、alpha 网格和预测公式，然后运行：

```bash
conda run -n moe-steering python scripts/evaluate_margin_prediction.py \
  --config configs/olmoe_pilot.yaml \
  --layer 6 \
  --downstream-layer 7 \
  --split test \
  --out results/raw/margin_prediction_test.jsonl

conda run -n moe-steering python scripts/summarize_margin_prediction.py \
  --calibration results/raw/margin_prediction_calibration.jsonl \
  --test results/raw/margin_prediction_test.jsonl \
  --out results/summary/margin_prediction.json
```

继续条件在 `margin_prediction.json` 中表现为 `go: true`：AUROC 的 bootstrap 95% CI 下界高于 0.5，且 Brier 优于 test 常数基率预测。若失败，先缩小 alpha、缩短 horizon 并重做 T4；仍失败则转向非线性或结构性泄漏分析。

## 8. Stage 4A：从 train 构造目标方向

目标方向只能使用 train 反事实对：

```bash
conda run -n moe-steering python scripts/build_target_direction.py \
  --config configs/olmoe_pilot.yaml \
  --layer 6 \
  --out artifacts/target_layer6.pt
```

该 artifact 记录 train pair IDs、模型 revision、router hash、hook 和代码 commit。不能跨 layer、模型、revision 或 hook 复用。

## 9. Stage 4B：在 calibration 构建 DRPI 候选

先做一个小候选：

```bash
conda run -n moe-steering python scripts/build_drpi.py \
  --config configs/olmoe_pilot.yaml \
  --target-direction artifacts/target_layer6.pt \
  --layer 6 \
  --horizon 4 \
  --rank 8 \
  --limit 16 \
  --out artifacts/drpi_l6_h4_r8_smoke.pt \
  --report results/summary/drpi_l6_h4_r8_smoke.json
```

检查 `stop_target_removed` 和 `target_retention`。若目标方向保留率低于 20%，说明动态约束删除了超过 80% 的目标范数；停止复杂化方法并报告目标与路径保持不可兼得。

smoke 正常后去掉 `--limit`，并对计划中的 `horizon=[1,4,8]`、`rank=[4,8,16,32]` 生成独立 artifact。每个候选使用不同文件名，不能覆盖。

## 10. Stage 4C：构建公平的优化基线

三个方向优化基线使用相同 calibration 数量、step 数和学习率：

```bash
for mode in target_only output_kl retain_loss; do
  conda run -n moe-steering python scripts/build_optimized_baseline.py \
    --config configs/olmoe_pilot.yaml \
    --initial-direction artifacts/target_layer6.pt \
    --layer 6 \
    --mode "$mode" \
    --steps 32 \
    --limit 16 \
    --out "artifacts/${mode}_layer6.pt"
done
```

正式比较时，DRPI 和优化基线必须使用相同 calibration 数据预算与前向/梯度预算。直接 router bias、full、static blind、route-visible 和随机等维 blind 基线由统一评测脚本自动生成。

## 11. Stage 4D：只用 validation 选择候选

对每个 DRPI 候选分别运行 validation，输出到独立 JSONL。示例：

```bash
conda run -n moe-steering python scripts/evaluate_interventions.py \
  --config configs/olmoe_pilot.yaml \
  --target-direction artifacts/target_layer6.pt \
  --drpi-artifact artifacts/drpi_l6_h4_r8.pt \
  --split validation \
  --optimized-baseline target_only=artifacts/target_only_layer6.pt \
  --optimized-baseline output_kl=artifacts/output_kl_layer6.pt \
  --optimized-baseline retain_loss=artifacts/retain_loss_layer6.pt \
  --out results/raw/validation_l6_h4_r8.jsonl

conda run -n moe-steering python scripts/summarize_effect_matching.py \
  --input results/raw/validation_l6_h4_r8.jsonl \
  --reference static_blind \
  --candidate drpi \
  --horizon 4 \
  --out results/summary/validation_l6_h4_r8.json
```

只根据 validation 冻结 injection layer、horizon、dangerous rank、alpha/插值范围和正则设置。不要为了 test 结果重新选择这些值。

## 12. Stage 5：冻结后运行一次 test

以下假设 validation 选中了 `layer=6, horizon=4, rank=8`；实际命令应替换为冻结值：

```bash
conda run -n moe-steering python scripts/evaluate_interventions.py \
  --config configs/olmoe_pilot.yaml \
  --target-direction artifacts/target_layer6.pt \
  --drpi-artifact artifacts/drpi_l6_h4_r8.pt \
  --split test \
  --optimized-baseline target_only=artifacts/target_only_layer6.pt \
  --optimized-baseline output_kl=artifacts/output_kl_layer6.pt \
  --optimized-baseline retain_loss=artifacts/retain_loss_layer6.pt \
  --out results/raw/test_frozen_l6_h4_r8.jsonl

conda run -n moe-steering python scripts/summarize_effect_matching.py \
  --input results/raw/test_frozen_l6_h4_r8.jsonl \
  --reference static_blind \
  --candidate drpi \
  --horizon 4 \
  --bootstrap 10000 \
  --out results/summary/effect_matching_test.json

conda run -n moe-steering python scripts/plot_effect_matching.py \
  --input results/summary/effect_matching_test.json \
  --out results/summary/effect_matched_path_divergence.png
```

报告时同时查看：

- `target_effect`：目标 token log-prob 改变量；
- `topk_jaccard`、`routing_js`：路径中介指标；
- `output_kl`、`next_token_nll_delta`：真实外部损失代理；
- paired bootstrap 95% CI 和配对置换检验；
- same-route/different-behavior 与 different-route/same-behavior 四象限计数。

只有 DRPI 在相同目标效果下优于 static blind、普通 steering、直接 output-KL 和 retain-loss 约束，才可升级为“减少副作用的方法”。若只减少路径分歧，应称为 route-control 工具；若路径与外部损失无关，应转为 route-trace 因果解释边界研究。

## 13. 结果文件与恢复规则

| 文件 | 含义 | 是否可覆盖 |
|---|---|---|
| `results/raw/*.jsonl` | 逐样本原始结果 | 不可覆盖，只追加或换新文件名 |
| `results/summary/*.json` | 由原始记录产生的报告 | 可重新生成，但不得手改数值 |
| `results/summary/*.png` | 脚本生成图 | 可由相同原始数据重新生成 |
| `artifacts/*.pt` | 方向、basis、DRPI 或基线 | 不可跨模型/revision/hook 复用 |

长任务中断后，用完全相同的命令和 `--out` 继续；`leakage_curve.py` 会读取已完成键并跳过。其他评测脚本当前采用追加写入，因此重跑前应改用新的输出文件名，避免重复样本进入统计。

## 14. 推荐的实际执行批次

不要一次启动完整网格。建议按以下批次执行：

1. `pytest` 和 MPS 最小张量检查；
2. 完成权重并通过 `inspect_model.py`；
3. `leakage_curve --limit 1`；
4. `leakage_curve --limit 8` 和 pilot 图；
5. T4 `--limit 8`；
6. calibration/test margin prediction；
7. train target direction；
8. 单个 DRPI candidate `--limit 16`；
9. 完整 calibration DRPI 候选；
10. validation 选参；
11. 冻结配置后只运行一次 test；
12. 汇总、作图并记录所有 No-Go 或负结果。

## 15. 当前最先要做的事情

本项目现在应从第 2 节继续：完成三个 OLMoE 权重分片。权重完成后首先重跑 `inspect_model.py`。在 `router_hook_report.json` 的 `status` 变为 `passed` 前，不要运行泄漏、Jacobian 或 DRPI 实验。

